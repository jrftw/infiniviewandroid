// Top-level build script for common configurations if needed:
plugins {
    // Usually nothing or just code-quality plugins if you have them.
    // DO NOT apply "com.android.application" or "org.jetbrains.kotlin.android" here.
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // other repositories if needed
    }
}
